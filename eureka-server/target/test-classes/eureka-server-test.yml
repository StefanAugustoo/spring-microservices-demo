spring:
  application:
    name: eureka-server
  cloud:
    config:
      import-check:
        enabled: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 0
