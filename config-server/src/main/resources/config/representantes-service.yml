server:
  port: 8083

spring:
  application:
    name: representantes-service
  datasource:
    url: jdbc:h2:mem:representantesdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
